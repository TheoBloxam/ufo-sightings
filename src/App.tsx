import { Stack } from "@mui/material"
import { AppHeading } from "./components/AppHeading"

export const App = () => {
  return (
    <Stack
      spacing={4}
      padding={4}>
      <AppHeading />
    </Stack>
  )
}
